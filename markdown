# This is an `<h1>` heading, which is the largest

## This is an `<h2>` heading

###### This is an `<h6>`heading, which is the smallest

---

## About this file

This file demonstrates common Markdown features: headings, emphasis, lists, code blocks, links, images, tables, and task lists. Use these examples as a quick reference.

### Emphasis

- *Italic text* is created with single asterisks or underscores.
- **Bold text** uses double asterisks or underscores.
- ***Bold + Italic*** can combine both.

### Code

Inline code: `const x = 1`

Fenced code block (JavaScript):
```javascript
function greet(name) {
  return `Hello, ${name}!`;
}
console.log(greet('world'));
